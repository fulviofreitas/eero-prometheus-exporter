# ============================================================================
# 🚀 Automated Semantic Release
# ============================================================================
# Automatically creates releases based on conventional commit messages.
# Uses reusable actions from workflow-arsenal for maintainability.
#
# Chain: CI → Release → Publish TestPyPI → Publish PyPI → Notify eero-ui
# Triggers: After CI Pipeline succeeds on main/master
# ============================================================================

name: 🚀 Release

on:
  workflow_run:
    workflows: ["🧪 CI Pipeline"]
    types: [completed]
    branches: [master]
  workflow_dispatch:
    inputs:
      dry_run:
        description: "🧪 Dry-run mode (no actual release)"
        required: false
        default: "false"
        type: choice
        options:
          - "false"
          - "true"

concurrency:
  group: release
  cancel-in-progress: false

permissions:
  contents: write
  issues: write
  pull-requests: write
  # id-token: write is intentionally NOT set at workflow level.
  # OIDC trusted publishing is scoped only to the publish-testpypi and publish-pypi jobs,
  # which are bound to GitHub Environments (testpypi/pypi) with branch protection.
  # See CVE-2026-45321 (Mini Shai-Hulud worm) for the abuse pattern this mitigates.

jobs:
  # ============================================================================
  # 🚀 Semantic Release
  # ============================================================================
  release:
    name: 🚀 Semantic Release
    runs-on: ubuntu-latest
    timeout-minutes: 15

    if: |
      (github.event_name == 'workflow_dispatch') ||
      (github.event.workflow_run.conclusion == 'success' &&
      !contains(github.event.workflow_run.head_commit.message, '[skip ci]') &&
      !contains(github.event.workflow_run.head_commit.message, 'chore(release)'))

    outputs:
      released: ${{ steps.release.outputs.released }}
      version: ${{ steps.release.outputs.version }}

    steps:
      - name: 🔑 Generate GitHub App token
        id: app-token
        uses: actions/create-github-app-token@v3
        with:
          client-id: ${{ secrets.RENOVATE_APP_ID }}
          private-key: ${{ secrets.RENOVATE_APP_PRIVATE_KEY }}

      - name: 📥 Checkout repository
        uses: actions/checkout@v7
        with:
          fetch-depth: 0
          persist-credentials: false
          token: ${{ steps.app-token.outputs.token }}

      - name: 🚀 Run semantic-release
        id: release
        uses: fulviofreitas/workflow-arsenal/.github/actions/semantic-release@master
        with:
          github-token: ${{ steps.app-token.outputs.token }}
          dry-run: ${{ github.event.inputs.dry_run || 'false' }}

  # ============================================================================
  # 📦 Publish to TestPyPI (validation step)
  # ============================================================================
  publish-testpypi:
    name: 📦 Publish to TestPyPI
    runs-on: ubuntu-latest
    needs: release
    if: |
      success() &&
      needs.release.outputs.released == 'true' &&
      needs.release.outputs.version != '' &&
      github.event.inputs.dry_run != 'true'
    timeout-minutes: 10

    environment:
      name: testpypi
      url: https://test.pypi.org/project/eero-prometheus-exporter/

    permissions:
      id-token: write  # Required for Trusted Publishing
      contents: read

    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v7
        with:
          ref: master
          fetch-depth: 1

      - name: 🐍 Set up Python
        uses: actions/setup-python@v7
        with:
          python-version: "3.14"

      - name: 🏷️ Create dev version
        id: version
        run: |
          DEV_VERSION="${{ needs.release.outputs.version }}.dev0"
          echo "dev_version=${DEV_VERSION}" >> $GITHUB_OUTPUT

          # Update pyproject.toml
          sed -i "s/^version = \"[^\"]*\"/version = \"${DEV_VERSION}\"/" pyproject.toml

          # Update __init__.py
          sed -i "s/__version__ = \"[^\"]*\"/__version__ = \"${DEV_VERSION}\"/" src/eero_exporter/__init__.py

          echo "📦 TestPyPI version: ${DEV_VERSION}"

      - name: 📦 Install build tools
        run: |
          python -m pip install --upgrade pip
          pip install build twine

      - name: 🏗️ Build package
        run: python -m build

      - name: 🔍 Validate package
        run: twine check dist/*

      - name: 📤 Publish to TestPyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          repository-url: https://test.pypi.org/legacy/
          skip-existing: true
          attestations: false
          verbose: true

  # ============================================================================
  # 📦 Publish to PyPI (production)
  # ============================================================================
  publish-pypi:
    name: 📦 Publish to PyPI
    runs-on: ubuntu-latest
    needs: [release, publish-testpypi]
    if: |
      success() &&
      needs.release.outputs.released == 'true' &&
      needs.release.outputs.version != '' &&
      github.event.inputs.dry_run != 'true'
    timeout-minutes: 10

    environment:
      name: pypi
      url: https://pypi.org/project/eero-prometheus-exporter/

    permissions:
      id-token: write  # Required for Trusted Publishing
      contents: read

    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v7
        with:
          ref: master
          fetch-depth: 1

      - name: 🐍 Set up Python
        uses: actions/setup-python@v7
        with:
          python-version: "3.14"

      - name: 📦 Install build tools
        run: |
          python -m pip install --upgrade pip
          pip install build twine

      - name: 🏗️ Build package
        run: python -m build

      - name: 🔍 Validate package
        run: twine check dist/*

      - name: 📤 Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          attestations: false
          verbose: true

  # ============================================================================
  # 🔔 Notify Downstream Repositories
  # ============================================================================
  notify-downstream:
    name: 🔔 Notify eero-ui
    runs-on: ubuntu-latest
    needs: [release, publish-pypi]
    if: |
      success() &&
      needs.release.outputs.released == 'true' &&
      needs.release.outputs.version != '' &&
      github.event.inputs.dry_run != 'true'
    timeout-minutes: 5

    steps:
      - name: 🔔 Notify eero-ui
        uses: fulviofreitas/workflow-arsenal/.github/actions/notify-downstream@master
        with:
          app-id: ${{ secrets.RENOVATE_APP_ID }}
          app-private-key: ${{ secrets.RENOVATE_APP_PRIVATE_KEY }}
          target-repo: eero-ui
          package: eero-prometheus-exporter
          version: ${{ needs.release.outputs.version }}
